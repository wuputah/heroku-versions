<html>
<head>
  <title>Heroku versions: <%= ENV['STACK'] %></title>
</head>

<body>
  <h1><%= ENV['STACK'] %></h1>

  <p>
    <a href="http://aspen-versions.heroku.com/">aspen-mri-1.8.6</a> |
    <a href="http://bamboo-ree-versions.heroku.com/">bamboo-ree-1.8.7</a> |
    <a href="http://bamboo-mri191-versions.heroku.com/">bamboo-mri-1.9.1</a> |
    <a href="http://bamboo-mri192-versions.heroku.com/">bamboo-mri-1.9.2</a> |
    <a href="http://github.com/jdance/heroku-versions">source code</a>
  </p>

  <pre>
<strong><%= defined?(RUBY_DESCRIPTION) ? RUBY_DESCRIPTION : "ruby #{RUBY_VERSION} (#{RUBY_RELEASE_DATE} patchlevel #{RUBY_PATCHLEVEL}) [#{RUBY_PLATFORM}]" %>
<%= "rubygems #{Gem::VERSION}" %></strong>

<strong>$ env</strong>
<%= ENV.collect { |k, v| "#{k}=#{v}" }.join("\n") %>

<strong>$ gem list</strong>
<%= `gem list` %>

<strong>$ dpkg -l</strong>
<%=
  `dpkg -l | grep ^ii`.split("\n").collect { |s| s.split(/  +/) }.collect { |pkg| "#{pkg[1]} (#{pkg[2]})" }.join("\n")
%>
  </pre>

<p><a href="/evil">Nobody expects the Spanish Inquisition!</a></p>

</body>

</html>
